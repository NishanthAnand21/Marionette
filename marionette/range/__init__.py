"""The Marionette range: deliberately vulnerable targets, no external services.

``vulnerable_mcp_server``  MCP over stdio (subprocess)
``http_mcp_server``        the same tools over HTTP, ephemeral localhost port
"""
